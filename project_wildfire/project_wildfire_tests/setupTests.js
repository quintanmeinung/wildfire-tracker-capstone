// Mock Leaflet's CSS (optional, to avoid console warnings)
//jest.mock('leaflet/dist/leaflet.css', () => ({}));

// Extend Jest matchers (optional, but useful)
//require('@testing-library/jest-dom');
